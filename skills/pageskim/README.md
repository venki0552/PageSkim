# PageSkim skill (placeholder)

Arrives in **Phase 6**: a `SKILL.md` + scripts that let an AI coding assistant execute "make my site agent-readable" end to end — detect the site generator (plain HTML / Astro / Next.js / Hugo / Jekyll), run `pageskim generate` over the build output, wire a build step plus CI validation, and emit the site index.
